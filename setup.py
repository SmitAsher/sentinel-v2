from setuptools import setup, find_packages

setup(
    name="sentinel-v2",
    version="2.0.0",
    description="Network packet analysis with TLS decryption",
    author="SmitAsher",
    packages=find_packages(),
    python_requires=">=3.8",
    install_requires=[],
)
